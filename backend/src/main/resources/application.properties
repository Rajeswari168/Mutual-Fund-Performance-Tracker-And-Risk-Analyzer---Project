server.port=${PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/mutual_funds_db?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USER:root}
spring.datasource.password=${DB_PASS:Raji@2006}

# Auto-initialize schema from schema.sql
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true
